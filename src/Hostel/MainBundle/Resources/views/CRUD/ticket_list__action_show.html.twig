{% if admin.id(object) and admin.isGranted('VIEW', object) and admin.show|length > 0 %}
	<a class="btn btn-sm btn-default show_link" href="{{ path('ticket', {id: object.id}) }}" target="_blank">
		<i class="fa fa-eye"></i>
		{{ 'link_action_show'|trans({}, 'SonataAdminBundle') }}</a>
{% endif %}